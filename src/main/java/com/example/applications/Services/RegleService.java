/*
package com.example.applications.Services;

import com.example.applications.entities.ActionRegle;
import com.example.applications.entities.Regle;
import com.example.applications.entities.TypeRegle;
import org.springframework.stereotype.Service;

import java.util.List;

public interface RegleService {

    List<Regle> getAllRegle ();
    Regle addRegle (Regle regle , ActionRegle ActionRegle);
    public void deleteRegle(Long id);
    Regle findByID (Long id);
    Regle updateRegle(Regle regle ,Long id) ;
}
*/
